# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
# standard build
  - board: seeeduino_xiao_ble # seeeduino_xiao_ble for v0.3.0 Vs xiao_ble//zmk for main
    shield: totem_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
  - board: seeeduino_xiao_ble
    shield: totem_right
  - board: seeeduino_xiao_ble
    shield: settings_reset

# +2dB version
  - board: seeeduino_xiao_ble
    shield: totem_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n -DCONFIG_BT_CTLR_TX_PWR_PLUS_2=y
    artifact-name: totem_left_2dB
  - board: seeeduino_xiao_ble -DCONFIG_BT_CTLR_TX_PWR_PLUS_2=y
    shield: totem_right
    artifact-name: totem_right_2dB

##    experimental = 1MBit, do not want to use it right now
#  - board: seeeduino_xiao_ble
#    shield: totem_left
#    snippet: studio-rpc-usb-uart
#    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n -DCONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y -DCONFIG_BT_CTLR_TX_PWR_PLUS_3=y
#    artifact-name: totem_left_experimental_3dB
#  - board: seeeduino_xiao_ble
#    shield: totem_right
#    cmake-args: -DCONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y -DCONFIG_BT_CTLR_TX_PWR_PLUS_3=y
#    artifact-name: totem_right_experimental_3dB
